export const addTabs = () => {
  // 添加标签页逻辑
  console.log("Adding tabs");
};

export const displayResults = (data, isHistory = false) => {
  // 显示结果逻辑
  console.log("Displaying results:", data, isHistory);
};
